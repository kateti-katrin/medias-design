#!/usr/bin/env ruby
# Development server: build assets then start Rails

system("npm run build") || abort("JS build failed")
system("npm run build:css") || abort("CSS build failed")
exec "./bin/rails", "server", "-p", "3000", *ARGV
